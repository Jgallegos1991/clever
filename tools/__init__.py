"""
tools package initializer.

Why: Treat the tools directory as a proper Python package so runtime modules
     (e.g., introspection) can import helper utilities like clever_doctor.
Where: Imported implicitly when accessing utilities via `from tools import ...`.
How: Minimal stub keeping documentation style consistent.
"""
